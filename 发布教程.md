# 🚀 四川旅游网站 — 零基础发布教程

你现在有两个文件：
- `index.html` — 网站本体
- `README.md` — 说明文档

要把它们变成一个别人能访问的网址，最简单的方法是 **GitHub Pages**（免费、无需服务器）。

---

## 准备工作（5 分钟）

### 1. 注册 GitHub 账号
打开 https://github.com ，点击右上角 **Sign up**。
- 填邮箱、密码、用户名（建议用英文，比如 `lzx-travel`）
- 验证邮箱
- 登录后看到 Dashboard 就完成了

---

## 方法 A：网页上传（最简单 ⭐推荐）

这个方法不需要安装任何工具，全程在浏览器操作。

### 第 1 步：创建仓库
1. 登录 GitHub 后，点击右上角 **+** 号 → **New repository**
2. 在 **Repository name** 填 `sichuan-travel`（名字随意，英文即可）
3. **不要**勾选 "Add a README file"（因为我们已经有了）
4. 点击底部绿色 **Create repository** 按钮

### 第 2 步：上传文件
1. 页面跳转后，看到 "Quick setup" 页面，点击中间蓝色的 **uploading an existing file** 链接
2. 把 `index.html` 和 `README.md` 两个文件**拖拽**到浏览器窗口
3. 等待进度条走完，下方 **Commit message** 随便写（如 "上传网站"）
4. 点击绿色 **Commit changes** 按钮

### 第 3 步：开启 Pages
1. 仓库页面顶部，点击 **Settings** 标签
2. 左侧菜单找到 **Pages**（在 "Code and automation" 下面）
3. **Branch** 下拉框选择 `main`（或 `master`），右边选 `/ (root)`，点击 **Save**
4. 等 1–2 分钟，刷新页面，会显示：
   > ✅ Your site is live at `https://你的用户名.github.io/sichuan-travel/`
5. 点击那个链接，就是你的网站了！

### 第 4 步：分享
把 `https://你的用户名.github.io/sichuan-travel/` 发给任何人，他们在浏览器打开就能用。

---

## 方法 B：命令行推送（适合后续更新）

如果以后要更新网站内容，用命令行更快：

### 第 1 步：配置 Git 身份
打开 PowerShell，输入（替换成你的信息）：
```powershell
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@example.com"
```

### 第 2 步：关联远程仓库并推送
```powershell
cd "C:\Users\LZX\Documents\Codex\2026-06-11\new-chat\deploy"
git remote add origin https://github.com/你的用户名/sichuan-travel.git
git commit -m "首次发布"
git push -u origin master
```
会弹出一个窗口让你登录 GitHub，点 **Sign in with your browser** 授权即可。

### 第 3 步：开启 Pages
同方法 A 的第 3 步。

### 后续更新网站
修改代码后，在 deploy 目录执行：
```powershell
git add .
git commit -m "更新内容"
git push
```
1–2 分钟后网站自动更新。

---

## 常见问题

**Q: 访问网址显示 404？**
A: 等 2–3 分钟再刷新。如果还不行，去 Settings → Pages，确认 Source 选的是正确的分支。

**Q: 网址太长能不能短一点？**
A: 可以把仓库名取短一点（如 `sc`），网址就变成 `https://用户名.github.io/sc/`。或者绑定自己的域名（Settings → Pages → Custom domain）。

**Q: 能不能用中文仓库名？**
A: 不建议，网址里中文会被转码成一长串字符。

**Q: 更新了文件但网站没变化？**
A: 浏览器按 Ctrl+F5 强制刷新，或等 2 分钟后自动生效。

---

## 需要我帮忙？

如果哪一步卡住了，告诉我你在哪一步、看到什么提示，我帮你解决。